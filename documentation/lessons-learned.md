# Lessons Learned

## Day 1

- Azure DevOps Scrum process: work items are called Product Backlog Items (PBIs), estimation field is called Effort — not Story Points
- gh CLI handles repo creation + cloning in a single command — no need to create repo on github.com first
- .gitkeep files are needed to track empty folders in Git

